# 知行 · 无行动，不知乎

知乎黑客松 2026 参赛作品。读取用户知乎收藏夹，用 AI 分拣出「能做的」和「值得懂的」，
转成两分钟行动卡与闪卡，按艾宾浩斯间隔每日推送，完成后可一句话回访作者。

- 规范文档：`docs/superpowers/specs/`（设计）、`docs/superpowers/plans/`（实施计划）
- 运行：`bun install && bun run dev`，需要 `.env.local`（见 `.env.example`）
- 测试：`bun run test`
- 凭证只放 `.env.local`，不进代码、日志、截图
